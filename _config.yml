# Site Build Settings

# Jekyll-paginate-v2 configuration
#  pagination:
#   enabled: true
#   per_page: 5
#   permalink: '/page/:num/'
#   title: ':title - page :num of :max'
#   limit: 0
#   sort_field: 'date'
#   sort_reverse: true

# jekyll-paginate configuration
paginate:            5
lang: pl

# Build settings
markdown:            kramdown
highlighter:         rouge
permalink:           /:title
plugins:             [jekyll-paginate, jekyll-sitemap, jekyll-feed, jekyll-seo-tag, jekyll-archives]
whitelist: jekyll-archives

# Customise atom feed settings (this is where Jekyll-Feed gets configuration information)
title:		           'Mundus Barbaricus'
description:	       'Nieregularnie prowadzony blog o wiekach średnich'
author:              'Mundus Barbaricus'

# RSS 2.0 can be used instead of Atom by uncommenting following two lines
#feed:
#  path: rss-feed.xml
# Rybbit Analytics Configuration
rybbit_instance_url: "https://app.rybbit.io" # Optional, defaults in include
rybbit_site_id: "1737"
